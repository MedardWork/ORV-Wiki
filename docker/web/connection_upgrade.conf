# Maps the incoming `Upgrade` header to the right value of the `Connection`
# header for upstream — required by SignalR / WebSocket proxying.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
